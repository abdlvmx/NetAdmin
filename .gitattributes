# Значок продукта и ресурс, который вшивает его в .exe. Двоичные файлы git
# обычно узнаёт сам по нулевым байтам, но полагаться здесь на догадку нельзя:
# при core.autocrlf=true подмена CRLF внутри .ico ломает значок молча — файл
# остаётся на месте и продолжает выглядеть целым.
*.ico  binary
*.syso binary
*.png  binary
*.db   binary
